/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains:[
          "localhost",
          "digital-hippo-production-b47c.up.railway.app"
        ]
      },
}

module.exports = nextConfig
